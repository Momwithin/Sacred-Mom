export default function SpeakToMom() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Speak to MOM</h1>
      <p>This is the main interface where sacred conversations will unfold.</p>
    </div>
  );
}
// Speak to MOM UI placeholder